export type processProps = {
    id: number,
    processKey: string,
    deadline: string,
    materia: string,
    name: string,
    distributionDate: string,
    status: string
    conclusionDate: string,
    userId: number,
    categoryId: number
}